# 发布记录

## 1.0.0-rc5 — 2026-08-05

### 数据源边界修正

- 按教师要求移除非指定来源的真实 ADS-B 数据、派生文件、下载脚本和专用测试。
- 本次不新增 OpenSky 数据；OpenSky 数据准备由对应助教另行完成并验收。
- 原有合成教学数据继续用于固定边界值、异常和预期结果测试，不将其表述为真实数据。

## 1.0.0-rc3 — 2026-07-30

### 助教 A 环境加固

- 环境检查新增课程包 `.venv` 路径和 `include-system-site-packages = false` 验证，避免误用本机原始环境。
- SQLite 调整为非阻断选做检查；不可用时输出 `WARN`，CSV 必做路径仍可完成 M2-M6 全链。
- 端到端试跑拆分为 CSV 必做路径和 SQLite 选做路径，分别核对 13 项和 14 项成果。
- 文件冒烟测试新增 M4 预生成候选的路径、字段和内容检查。
- 环境说明补充 wheelhouse 离线安装步骤、常见错误和降级处理；新增 B/C 独立环境试跑模板。

### A 独立验收

- Windows 11、Python 3.12.7 独立 `.venv`；必做环境 6/6、SQLite 选做 1/1。
- 文件冒烟 6/6、发布清单 3/3、自动化测试 10/10。
- M2-M6 CSV 必做链和 SQLite 选做链均通过；详细记录见 `test_records/a_environment_fallback_trial_2026-07-30.md`。

### 发布边界

- `environment/wheelhouse/` 尚需在与机房相同的操作系统和 Python 版本上生成，或由学校确认机房已预装依赖。
- B/C 仍需分别完成干净 Windows 环境和仅使用学生材料的独立试跑，完成前不得标记环境冻结。

## 1.0.0-rc2 — 2026-07-29

### 助教 A 集成整理

- 明确五类 README 的受众和职责：仓库导航、统一环境、学生任务、助教参考、M6 提交模板各自独立，删除重复安装说明。
- 将 `environment/README_environment.md` 作为唯一环境说明；正式运行命令显式使用课程包 `.venv`，不依赖本机已有第三方包。
- 更新文件版本清单、学生/助教包入口、M6 运行模板和问题闭环记录。

### A 独立验收

- Windows 11、Python 3.12.7 独立 `.venv`，`include-system-site-packages = false`。
- pandas 2.3.3、matplotlib 3.11.1；环境 6/6、冒烟 5/5、清单 3/3、自动化测试 10/10、M2-M6 端到端试跑全部通过。
- 详细记录见 `test_records/a_environment_trial_2026-07-29.md`。

### 发布边界

- 当前仍为助教联调候选版；B/C 交叉课堂身份计时试跑完成后，由 A 汇总问题并执行最终冻结。
- 学生发布时必须排除 `ta_reference_package/` 和 `test_records/`。

## 1.0.0-rc1 — 2026-07-29

### 完成

- 建立可复现 `.venv` 部署脚本和五层检查入口；已在 Windows 11、Python 3.12.13、中文路径下通过。
- 补齐 M1-M5 离线数据、字段字典、41 字节 TeachingLink 规范、统一模型、模板、提示和模块代码骨架。
- 补齐 M2-M6 助教参考实现、官方检查点、预期结果、错误 case 和 10 项自动化测试。
- 完成端到端参考试跑：3 条可编码 OpenSky 记录、9 帧多时刻消息、3 个目标、6 条统一消息、5 条告警、SQLite 9 行。
- 修正 M6 运行路径、展示材料页数、PowerShell 5 编码兼容和定点量化测试判据。
- 增加三位助教分工、问题日志和独立试跑记录。

### 发布边界

- 当前为助教联调候选版；B/C 仍需交换模块做课堂身份计时试跑，A 完成最终冻结。
- 学生发布时必须排除 `ta_reference_package/` 和 `test_records/`。

## 0.1.0-dev — 2026-07-24

### 新增

- 按发布稿建立`summer_school_practice_v1.0`正式目录。
- 建立学生包、助教参考包、环境和试跑记录目录。
- 新增环境检查和JSON/二进制/CSV/NDJSON冒烟测试。
- 新增M6统一入口骨架、README模板、检查点切换说明和提交清单。

### 当前限制

- 此历史版本未集成 M1-M5 正式数据和实现，已由 1.0.0-rc1 替代。
