# 实验改进记录

## 2026-08-14：OpenSky 端到端实验

- 使用 OpenSky 官方接口的 3 个离线快照，共 71 条真实状态向量；保留原始响应、下载时间和 SHA-256。
- 新增 `experiment/run_opensky_experiment.py`，一次完成目标选择、41 字节 TeachingLink 编码、模拟发送、接收解码、当前态势更新、CSV/SQLite 保存和精度误差统计。
- 默认选择 10 个跨快照重复出现的目标，共处理 30 条记录；也可用 `--targets 1` 做最小演示。
- 自动化测试覆盖真实数据来源、帧完整性、校验和、收发数量、SQLite 行数、空态势到最终态势的变化以及定点量化误差上限。
- 删除与实验无关的管理流程说明，项目只保留环境、数据、程序、测试和结果。

## 既有实验能力

- M1：理解 OpenSky 输入、TeachingLink 通信和接收端处理流程。
- M2：解析状态向量，完成 41 字节协议编解码与异常校验。
- M3：处理多目标多时刻消息，生成航迹、当前态势、CSV 和 SQLite。
- M4：把不同来源字段映射为统一态势模型。
- M5：检测位置缺失、数据延迟、重复记录和航向越界。
- M6：从空输出目录连续运行 M2-M5 并生成完整结果。
